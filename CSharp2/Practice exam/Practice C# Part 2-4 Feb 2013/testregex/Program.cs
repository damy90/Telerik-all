using System;
using System.Linq;
using System.Text.RegularExpressions;

namespace testregex
{
    class Program
    {
        static void Main(string[] args)
        {
            /*string test = "(S B)(S F)(E) | (S B)(S F)(T 14 16)  | (S L)(S R)(B)";
            string newText = Regex.Replace(test, @"([^0-9])\s+", m => m.Groups[1].Value);
            Regex cordinate = new Regex("[0-9]+");
            Match ma = cordinate.Match(newText);
            Console.WriteLine(ma);
            ma=ma.NextMatch();
            Console.WriteLine(ma);*/
            string input = "We happy few we band";

        }
    }
}
